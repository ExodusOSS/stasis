// A TypeScript + JSX source entry, the way react-native-safe-area-context ships its `src`. oxc
// parses this as TSX purely from the .tsx extension (no --jsx flag needed to PARSE it); --jsx is
// what makes the scanner CARRY it. The `: unknown` return annotation keeps it unmistakably
// TypeScript, and it imports nothing so it carries as a clean leaf.
export const SafeArea = (): unknown => <View>safe</View>
