// top-level isarray@2: distinct content from the nested @1 copy so we can
// tell the two apart when inspecting bundle output / resolutions.
module.exports = function isArrayV2(x) { return Array.isArray(x) }
module.exports.version = 2
