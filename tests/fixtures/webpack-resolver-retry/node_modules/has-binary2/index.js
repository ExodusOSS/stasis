// has-binary2 declares its own nested isarray@1 -- it should resolve to the
// node_modules/has-binary2/node_modules/isarray copy, not the top-level.
const isArray = require('isarray')
module.exports = function hasBinary(x) { return isArray(x) }
