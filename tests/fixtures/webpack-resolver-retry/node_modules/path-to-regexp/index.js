// path-to-regexp depends on top-level isarray@2 (no nested copy in its own
// node_modules), so this should resolve to node_modules/isarray.
const isArray = require('isarray')
module.exports = function pathToRegexp(x) { return isArray(x) }
