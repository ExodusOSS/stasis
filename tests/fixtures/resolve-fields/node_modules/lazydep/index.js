'use strict'

// Carries its own nested metro-runtime copy: the async-import helper resolves
// per parent (like Metro), so THIS file's injected edge must land on the nested
// copy while workspace files land on the hoisted one.
module.exports = function load() {
  return import('./inner.js')
}
