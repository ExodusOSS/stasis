'use strict'

// NESTED copy (under lazydep): content differs from the hoisted one so tests can
// tell the per-parent resolutions apart.
function asyncRequireNested(moduleID, paths, moduleName) {
  return require.importAll(moduleID)
}

module.exports = asyncRequireNested
