'use strict'

// A CUSTOM async-import helper (--asyncRequireModulePath=asyncshim) that itself uses a
// dynamic import(): exercises the injection fixpoint -- it must get its own injected
// edge (onto itself) and pull ./extra.js into the bundle.
module.exports = function asyncShim(moduleID) {
  return import('./extra.js')
}
